export const SCENE_LIST = "SCENE_LIST";

export enum Action {
    RESET = "reset",
    REPLAY = "replay",
    CAPTURE = "capture",
    DESCRIBE = "describe",
    NO_COMMAND = "no_command",
    CLEAR = "clear",
}
